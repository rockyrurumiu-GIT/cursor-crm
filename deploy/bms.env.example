# Copy to /etc/bms/bms.env and fill in secrets.
# sudo chmod 600 /etc/bms/bms.env && sudo chown root:root /etc/bms/bms.env
#
# Generate session secret:
#   python3.11 -c "import secrets; print(secrets.token_urlsafe(64))"

CRM_DB_URL=sqlite:////home/rocky/BMS/crm_v8.db
CRM_SESSION_SECRET=CHANGE_ME
CRM_COOKIE_SECURE=1
CRM_ALLOW_DEFAULT_ADMIN=0
# CRM_AUTH_MODE=rbac
